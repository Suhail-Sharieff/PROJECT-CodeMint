# Whats added in v5?
- initially i thought like okay since my app is Write heavy, it wud be better to replace MySQL with wide column db that uses LSM trees  like Apache Cassandra, but then i relalized that then i would have to implement complex application lvl logic to join data from multiple normalized tables and handle denormalization as well, since complex joins are used for computation at various places (especially in tests and battles)
- 